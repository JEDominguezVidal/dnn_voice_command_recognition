#!/usr/bin/env python
PACKAGE = "dnn_voice_command_recognition"

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

gen.add("rate",          double_t, 0, "Main thread rate", 10, 1, 100)
gen.add("FRAMES_PER_BUFFER", int_t, 0, "Samples per buffer", 4000, 100, 20000)
gen.add("command_list",  str_t,    0, "Comma-separated command list", "down,go,left,no,off,on,right,stop,up,yes")

# Overlapping window parameters
gen.add("buffer_seconds", double_t, 0, "Total audio buffer duration (seconds)", 2.0, 0.5, 5.0)
gen.add("window_seconds", double_t, 0, "Processing window duration (seconds)", 1.0, 0.1, 3.0)
gen.add("step_seconds",   double_t, 0, "Step size between windows (seconds)", 0.5, 0.01, 1.0)

# Whisper model selection
model_enum = gen.enum([
    gen.const("medium", str_t, "medium", "Whisper medium model"),
    gen.const("small", str_t, "small", "Whisper small model (default)"),
    gen.const("base", str_t, "base", "Whisper base model"),
    gen.const("tiny", str_t, "tiny", "Whisper tiny model")
], "Select Whisper model size")

gen.add("model_size", str_t, 0, "Whisper model size", "small", edit_method=model_enum)

exit(gen.generate(PACKAGE, "dnn_voice_command_recognition", "Node"))
